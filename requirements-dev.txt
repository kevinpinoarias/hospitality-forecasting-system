# Additional dependencies needed to run the full pipeline (training,
# baselines, evaluation plots) and the portfolio walkthrough notebook.
# Not needed to run the API - see requirements.txt for that.
#
# Install both for full local development:
#   pip install -r requirements.txt -r requirements-dev.txt

matplotlib>=3.7
statsmodels>=0.14
jupyter>=1.0
notebook>=7.0
