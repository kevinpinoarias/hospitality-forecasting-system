# Exact library versions that produced every figure in docs/EXPERIMENT_LOG.md
# (Python 3.14). The requirements files only set minimum versions, and
# gradient-boosting libraries can change results between releases: moving
# from xgboost 3.2.0 to 3.4.1 shifted one logged XGBoost figure by about £13,
# while the CatBoost-based results were unchanged. Install with:
#
#   pip install -r requirements.txt -r requirements-dev.txt -c constraints-experiments.txt

pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
catboost==1.2.10
lightgbm==4.7.0
prophet==1.4.0
cmdstanpy==1.3.0
statsmodels==0.14.6
torch==2.14.0
holidays==0.93
